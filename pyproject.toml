[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "intentlang"
version = "0.1.0"
dependencies = ["lark"]

[project.scripts]
intent = "intentlang.cli:main"
